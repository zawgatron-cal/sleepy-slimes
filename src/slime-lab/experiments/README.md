# Experiments

Drop one-off R3F / shader prototypes here (e.g. PBR brick sphere, custom `shaderMaterial`).

Wire them into `SlimeScene.tsx` when you want to compare side-by-side with the default `SlimeMesh`.

Reference textures live in `assets/reference/PavingStones092_1K-PNG/` if you revisit material mapping.

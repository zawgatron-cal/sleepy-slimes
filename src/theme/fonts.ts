/**
 * App-wide display font (Itim — rounded handwritten Google Font).
 * Loaded in `app/_layout.tsx`. Most UI uses `createAppStyles` so Text styles get `fontFamily` automatically
 * (RN’s `Text` no longer applies `Text.defaultProps`). Use `APP_FONT_FAMILY` for React Navigation titles / tab labels.
 */
export const APP_FONT_FAMILY = 'Itim_400Regular';
